#include <stdio.h>
#include "mat.h"

int main(int argc, char const *argv[])
{
    printf("Area do quadrado %d\n", areaSquare(6));
    printf("Area do retangulo %d\n", areaRectangle(6,4));
    return 0;
}
