// Faça um programa que possua um vetor denominado A que armazene 
// 6 numeros inteiros o programa deve executar os seguintes numero

// (a) Atribua os seguintes valores a esse vetor: 1, 0,5,-2,-5,7
// (b) Armazene em uma variavel inteira (simples) a soma entre os valores das posições A[0], A[1], A[5] do vetor
// e mostre na tela esta soma
// (c) mostre na tela o vetor na posição 4 atribuindo a esta posição o valor 100.

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define TAM 6;

int main(int argc, char const *argv[])
{
    for (int i = 0; i < TAM; i++)
    {
        printf("Digite um numero %d", i);
        scanf("%d", numbers)
    }
        
    return 0;
}
